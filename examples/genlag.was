end_time = 10
dt = 0.01 # take a small time step

# a ramp of slope 1 between 0 and 1 starting a t=1
y = limit(heaviside(t-1)*(t-1), 0, 1)
# compute a first-order lag with a certain characteristic time
z = lag(y, 1.23456789)

# print only one sample every ten time steps
PRINT t y z SKIP_STEP 10
